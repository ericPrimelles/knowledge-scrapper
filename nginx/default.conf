worker_processes auto;
pid /run/nginx/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  # Performance tuning
  sendfile on;
  tcp_nopush on;
  tcp_nodelay on;
  keepalive_timeout 65;
  client_max_body_size 10M;

  # Logging
  access_log /var/log/nginx/access.log;
  error_log  /var/log/nginx/error.log warn;

  # Compression
  gzip on;
  gzip_min_length 512;
  gzip_comp_level 1;
  gzip_types application/json application/javascript text/css text/plain text/xml;

  # Optional Docker resolver (if using ECS or Docker)
  resolver 127.0.0.11 ipv6=off;

  # Main server block
  server {
    listen 80;
    server_name _;

    # Health check endpoint for ALB
    location = /health {
      return 200 'OK';
      add_header Content-Type text/plain;
    }

    # Optional CORS handling
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization,Content-Type' always;

    if ($request_method = OPTIONS) {
      add_header 'Access-Control-Max-Age' 1728000;
      add_header 'Content-Type' 'text/plain charset=UTF-8';
      add_header 'Content-Length' 0;
      return 204;
    }

    # Main proxy to your app
    location / {
      proxy_pass http://localhost:3000;  # Adjust if your app runs elsewhere
      proxy_http_version 1.1;

      # Preserve headers (important for ALB and client IP)
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
}
